# Godot 4+ specific ignores
.godot/
/android/

# Exports
/build/
/export/
*.pck
*.zip

# Local-only project config (personal, not shared)
.devin/config.local.json

# macOS
.DS_Store

# Web export output
build/

# Playwright MCP screenshots
.playwright-mcp/

# Dead Godot scenes that Godot keeps restoring from .godot/exported/ cache
# even after deletion. Their referenced scripts/shaders are also gone, so
# they're harmless orphans — just ignored so they don't sneak back into
# commits.
/components/jack_visual.tscn
/components/socket_visual.tscn
/components/post_connect_box.tscn
